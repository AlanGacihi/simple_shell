#include "shell.h"

/**
 * main - Super simple shell
 *
 * Return: Always 0.
 */
int main()
{
	/*if (stdin)
	{
		read_and_exec();
		return (0);
		}*/

	while (1)
	{
		printf("> ");
		fflush(stdout);

		read_and_exec(0, 0);
	}

	return(0);
}
