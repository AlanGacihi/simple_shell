#include "shell.h"

/**
 * main - stat example
 *
 * Return: Always 0.
 */
int main(int ac, char **av)
{
	int i = 0;
	char *full_path;

	full_path = which("/bin/ls");

	if(full_path)
		printf("%s\n", full_path);
	else
		printf("Nah!\n");
}
